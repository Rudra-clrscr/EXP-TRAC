// Background animation is handled by ThemeContext which applies
// bg-waves / bg-fluid classes directly to document.body.
// This component is kept for compatibility but renders nothing.
const AnimatedBackground = () => null;

export default AnimatedBackground;
